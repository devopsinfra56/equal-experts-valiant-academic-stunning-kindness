# Async HTTP client — used to make outbound requests to the GitHub API
fastapi==0.136.0

# Async HTTP client — used to make outbound requests to the GitHub API
httpx==0.27.2

# ASGI server — the process that listens on port 8080 and passes requests to FastAPI
uvicorn==0.30.6

# Test runner — finds and runs all functions prefixed with test_
pytest==8.3.3

# Coverage plugin for pytest — generates coverage.xml for SonarCloud
pytest-cov==7.1.0

# Prometheus metrics — exposes /metrics for Prometheus scraping
prometheus-fastapi-instrumentator==7.1.0